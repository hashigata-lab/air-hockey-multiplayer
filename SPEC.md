# 4人対戦エアホッケー (4-Player Air Hockey) 仕様書

## 1. 概要
Node.jsとSocket.IOを利用した、リアルタイム4人対戦のウェブブラウザ向けエアホッケーゲーム。サーバーが物理演算と状態管理を行う権威サーバー（Authoritative Server）モデルを採用し、クライアントは描画と入力の送信に専念します。

## 2. システム構成・技術スタック
* **バックエンド (サーバー):**
  * Node.js
  * Express (静的ファイルの配信)
  * Socket.IO (リアルタイム双方向通信)
  * 物理演算: サーバーサイドでの独自実装（円と矩形・円同士の衝突判定）
* **フロントエンド (クライアント):**
  * HTML5 Canvas API (2D描画)
  * Socket.IO-client
  * Vanilla JavaScript (モジュール化)

## 3. ゲームルール・メカニクス
* **フィールド形状:** 正方形のフィールド。上下左右の4辺にそれぞれのプレイヤーのゴールが配置されます。
* **プレイヤー (マレット):** 
  * 1ルームにつき最大4人。
  * プレイヤーは自陣（割り当てられた辺の付近の一定エリア）のみマレットを操作可能です。
  * 操作方法: マウスまたはタッチ操作に追従します。
* **パック (Puck):**
  * フィールド内を物理法則（慣性、壁・マレットでの反射、摩擦による減速）に従って移動します。
* **勝敗・スコアリングルール (サバイバル形式):**
  * 各プレイヤーは初期ライフ（例: 5）を持ちます。
  * 自分のゴールにパックが入るとライフが1減ります。
  * ライフが0になったプレイヤーは脱落となり、そのプレイヤーのゴール領域はパックが反射する壁に変化します。
  * 最後に残った1人が勝者となります。

## 4. アーキテクチャと通信設計
### 4.1. サーバーサイド (ゲームループ)
* **Tick Rate:** サーバーは固定のインターバル（例: 60Hz = 約16.6ms）でゲームループを実行します。
* **処理フロー:**
  1. クライアントからの入力（マレットの現在/目標座標）の適用。
  2. 物理演算（パックの移動、壁やマレットとの衝突判定・反射処理）。
  3. ゴール判定とライフ更新。
  4. 最新のゲームステート（全プレイヤーの座標、パックの座標、スコア・ライフ情報）を全クライアントにブロードキャスト。

### 4.2. Socket.IO イベント設計
| イベント名 (方向) | ペイロード | 説明 |
| :--- | :--- | :--- |
| `join_game` (C→S) | `{ playerName }` | プレイヤーがゲームへの参加をリクエスト |
| `waiting_room` (S→C)| `{ playersCount }` | 待機室の状況（集まった人数など）を通知 |
| `game_start` (S→C) | `{ playerId, position }` | 4人揃った段階でゲーム開始。自身のプレイヤーIDと担当位置(top, bottom, left, right)を通知 |
| `player_input` (C→S)| `{ x, y }` | クライアントからサーバーへのマレット座標の送信（高頻度） |
| `game_state` (S→C) | `{ puck, players, lives }` | 毎フレームサーバーから送られるゲームの最新状態 |
| `goal_scored` (S→C) | `{ playerId, currentLives }` | 誰かがゴールを決められた際の通知（SE再生やエフェクトに利用） |
| `player_eliminated`(S→C)| `{ playerId }` | プレイヤーが脱落した通知 |
| `game_over` (S→C) | `{ winnerId }` | ゲーム終了と勝者の通知 |

## 5. 開発フェーズ (マイルストーン)
* **フェーズ 1: プロジェクトの初期設定と通信基盤構築**
  * Node.js + Express + Socket.IO環境の構築。
  * クライアント・サーバー間の簡単な接続確認。
* **フェーズ 2: クライアント側の描画と入力処理**
  * HTML5 Canvasを用いたフィールド、ゴール、マレット、パックの静的描画。
  * マウス操作によるマレットの座標取得。
* **フェーズ 3: サーバーサイドの物理演算とゲームループ**
  * サーバー側でのゲームループの実装。
  * パックの移動、壁での反射、マレットとの円同士の衝突判定の実装。
* **フェーズ 4: マルチプレイヤー同期の統合**
  * クライアントの入力をサーバーに送信。
  * サーバーの演算結果を受け取り、全クライアントで同一の画面を描画。
  * 4人のプレイヤーの参加と位置の割り当て処理（マッチメイキング）。
* **フェーズ 5: ゲームロジックの完成**
  * ゴール判定、ライフ管理、脱落処理の実装。
  * ゲーム開始・終了のフローと画面（待機画面、リザルト画面）の構築。

## 6. 追加開発フェーズ (友人と遊ぶためのプライベートルーム機能)
* **フェーズ 6: ロビーシステムとルーム管理**
  * **プライベートルーム作成**: URL（例: `?room=ABCD`）を使って、特定の友人のみを招待できる機能の実装。
  * **名前入力機能**: 参加時にプレイヤー名を入力し、ゲーム画面上に名前を表示して誰がどのマレットか分かるようにする。
  * **UIの改修**: ゲーム開始前に「部屋を作る」「部屋に入る」を選択できるタイトル画面（ロビー）の追加。
  * **観戦モードの拡張**: 5人目以降の友人が参加した場合、チャットなどでコミュニケーションを取りながら観戦できる機能。

## 7. オンライン公開（デプロイ）インフラ仕様
完成したゲームを実際にインターネット上で友人と遊べるようにするためのホスティングおよび運用仕様です。

* **推奨ホスティングプラットフォーム (PaaS):**
  * **Render / Railway / Fly.io** など（Node.js および WebSockets をネイティブサポートしているサービス）。
  * 理由: Socket.IO を利用したリアルタイム通信を持続的に行うため、サーバーレス関数（VercelやNetlify Functionsなど）ではなく、常時稼働するコンテナ環境が必要です。
* **通信プロトコル:**
  * `https://` および `wss://` (Secure WebSocket) での通信。
  * デプロイ先のプラットフォームが自動でSSL/TLS証明書を発行・終端（SSLオフロード）するものを利用します。
* **スケーリングに関する制約事項:**
  * **初期フェーズ（単一インスタンス構成）:** ルームごとのゲームステートをサーバーのオンメモリで管理しているため、最初はインスタンス数（サーバーの台数）を「1」に固定して運用します。
  * **将来的なスケールアウト対応:** アクセスが増えてサーバーを複数台構成にする場合は、`@socket.io/redis-adapter` などを導入し、さらにロードバランサー側で Sticky Sessions（セッション維持）を有効にする必要があります。
* **デプロイフロー:**
  * GitHub などのバージョン管理システムと連携し、`main` ブランチにプッシュされた際に自動で `npm install` および `npm start` が実行される CI/CD パイプラインを構築します。

## 8. 追加開発フェーズ (アイテム機能とユニーク効果)
* **フェーズ 8: アイテムシステムの導入**
  ゲームの予測不可能性とパーティーゲームとしての楽しさを高めるため、フィールドにランダムに出現する「アイテム」を実装します。

### 8.1. アイテムの基本メカニクス
* **出現条件:** 一定時間（例: 10〜20秒）ごとに、フィールド内の中央付近のランダムな座標にアイテムが出現します。
* **取得判定:** パックがアイテム（円形のオブジェクト）に衝突すると、アイテムが発動（取得）されます。
* **対象者の判定 (Last Hitter):** サーバーは常に「最後にパックに触れたマレット（プレイヤー）」を記録しておき、アイテムの効果はそのプレイヤーに対して発動します（一部の全体効果アイテムを除く）。
* **持続時間:** プレイヤーを強化・弱体化する効果は、一定時間（例: 10秒間）で元の状態に戻ります。

### 8.2. アイテムの種類とユニーク効果
以下のようないくつかの種類（色やアイコンで区別）を実装します。

1. **巨大化 (Size Up) - 緑色:**
   * 効果: 取得したプレイヤーのマレットの半径が1.5倍になり、ゴールを守りやすくなる。
2. **縮小化 (Size Down / Curse) - 紫色:**
   * 効果: 取得したプレイヤーのマレットの半径が半分になり、防御が困難になる（マイナスアイテム）。
3. **バリア (Barrier) - 青色:**
   * 効果: 取得したプレイヤーのゴールの少し前に、パックを跳ね返す「壁」が10秒間だけ出現する。
4. **スピードアップ (Speed Up) - 赤色:**
   * 効果: パック自体のスピードが急上昇し、全員にとって防御が難しくなる（全体効果）。
5. **マルチパック (Multi-Puck) - 虹色:**
   * 効果: アイテムを取得した瞬間、その場から「2つ目のパック」が出現する。追加パックは誰かのゴールに入った時点で消滅する。

### 8.3. サーバーとクライアントの責務（実装方針）
* **サーバー:** アイテムの出現タイマー管理、パックとの衝突判定、効果時間（`setTimeout`）の管理、状態変更のクライアントへの通知。
* **クライアント:** 追加された状態（マレットの大きさ変更、バリアの壁、アイテム自体の描画）の Canvas レンダリング。ローカルの予測描画時にもマレットの大きさを反映。

## 9. 追加開発フェーズ (効果音 - SE)
* **フェーズ 9: サウンドエフェクトの導入**
  ゲームの没入感とフィードバックを強化するため、各種アクションに合わせた効果音（SE）を実装します。

### 9.1. 必要なサウンド一覧
最低限、以下のオーディオファイル（`.mp3` または `.wav`）を用意・または生成して `public/sounds/` ディレクトリに配置します。
1. `hit.mp3` - マレットでパックを打った時のカンッという音
2. `wall.mp3` - パックが壁やバリアに反射した時のコンッという音
3. `goal.mp3` - 誰かのゴールにパックが入った時のブザーや歓声
4. `item_spawn.mp3` - アイテムがフィールドに出現した時のキラキラ音
5. `item_get.mp3` - アイテムを取得（効果発動）した時のパワーアップ音
6. `game_over.mp3` - ゲーム終了時のホイッスルなどの音

### 9.2. 発音の同期メカニクス（アーキテクチャ）
* 物理演算（衝突判定など）はすべてサーバー側で行われているため、**「いつ音を鳴らすか」の決定権もサーバー**にあります。
* **通信最適化:** 音を鳴らすためだけに個別の `socket.emit` を毎秒何度も送ると通信帯域を圧迫するため、毎フレーム送信している `game_state` の中に一時的な `events: []` 配列を持たせます。
* **処理フロー:**
  1. サーバーのゲームループ内で衝突（ヒット、壁反射、ゴールなど）が発生した場合、そのフレームの `events` 配列に文字列（例: `'hit'`, `'wall'`）を追加します。
  2. サーバーは `game_state` をクライアントに送信した後、次フレームのために `events` を空にリセットします。
  3. クライアントは `game_state` を受け取った際、`events` 配列をチェックし、含まれている文字列に対応する効果音を `Audio` オブジェクトで再生します。
* **ブラウザの自動再生ポリシー対応:** クライアント側では、ユーザーが画面をクリックまたはタップする（「部屋に入る」ボタンを押すなど）までは音が鳴らないよう、初回インタラクション時にAudioContextを初期化または無音再生する仕組みを入れます。

## 10. 追加開発フェーズ (UI/UX改修 - 体力表示の改善)
* **フェーズ 10: ライフ（体力）表示の視覚的アップデート**
  これまでの単なる数値とテキストによる体力表示を、よりゲームらしく直感的なUIに改修します。

### 10.1. 改善点と仕様
1. **ライフのアイコン化:**
   * 数字（例: `5`）での表示をやめ、残機をハートマーク（例: `❤️❤️❤️❤️❤️`）の数で表現します。
   * ダメージを受けるごとにハートが減っていく（または黒いハート `🖤` に変わる）視覚効果を取り入れます。
2. **描画位置と角度の最適化 (Left / Right):**
   * 現在、画面左右（Left, Right）のプレイヤーのテキストが水平に描画されており、フィールドと重なって見栄えが悪い問題を解消します。
   * Canvasの回転（`ctx.rotate`）を活用し、Leftプレイヤーの表示は左端に沿って縦向き（-90度回転）、Rightプレイヤーの表示は右端に沿って縦向き（90度回転）で描画するように修正し、プレイエリアを邪魔しないように配置します。
3. **プレイヤー名の表示:**
   * ハートアイコンの横（または上下）にプレイヤー名を配置し、誰の体力ゲージか一目で分かるように整理します。

## 11. 追加開発フェーズ (UI/UX改修 - アイテム演出の高度化)
* **フェーズ 11: アイテムUIのリッチ化**
  現在は単なる色付きの円と文字（`+`, `B` など）で表現されているアイテムの見た目を強化し、より直感的でゲームらしい演出を追加します。

### 11.1. 改善点と仕様
1. **リッチなアイコン（絵文字）の採用:**
   * 簡素なテキストから、視覚的に分かりやすい絵文字（Emoji）に変更します。
   * `SIZE_UP`: 🍄 または 🔼 (巨大化)
   * `SIZE_DOWN`: ☠️ または 🔽 (縮小・毒)
   * `BARRIER`: 🛡️ (盾・防御)
   * `SPEED_UP`: ⚡ (雷・加速)
   * `MULTI_PUCK`: ☄️ (分裂)
2. **フィールド上のアニメーション効果 (Pulsing):**
   * アイテムが静止しているだけでなく、少しずつ大きくなったり小さくなったりする「呼吸（パルス）」アニメーションや、淡く光るグロウ効果（`shadowBlur`）を追加し、プレイヤーの目を引くようにします。
3. **効果発動中のステータス表示:**
   * 現在は「誰が何のアイテム効果を受けているか」がフィールドの見た目（マレットの大きさなど）でしか判断できません。
   * プレイヤー名とハートの横に、現在受けている効果のアイコン（例: 🛡️）を小さく表示し、効果が切れるまでの「残り時間」が分かるリング（円グラフ）を重ねて描画することで、戦況をより把握しやすくします。

## 12. 追加開発フェーズ (ゲーム性拡張 - 必殺技システム)
* **フェーズ 12: 必殺技（ハイパースマッシュ）の導入**
  対戦をさらに白熱させるため、各プレイヤーに「SP（スペシャル）ゲージ」を持たせ、溜まったゲージを消費して放つ必殺技を追加します。

### 12.1. SPゲージの仕様
* **ゲージの蓄積:**
  * 各プレイヤーは 0〜100% のSPゲージを持ちます。
  * **ラリー蓄積:** パックを打ち返すたびに一定量（例: +10%）増加します。
  * **逆境ボーナス:** 自分のゴールにパックを入れられライフが減った際、ボーナスとして大きく（例: +30%）増加します。
* **UI表示:**
  * 体力（ハート）のすぐ下、またはマレットの周囲に、現在のSP量を示すゲージ（バーまたは円グラフ）を描画します。
  * 100%（MAX）になると、ゲージが光り、マレット自体にも「オーラ」のような視覚効果が付与されます。

### 12.2. 必殺技「ハイパースマッシュ」の仕様
* **発動条件 (UXの考慮):**
  * PCとスマートフォンの両方で操作を阻害しないよう、「専用ボタン」は設けません。
  * SPがMAX（100%）の状態で、**マレットを一定以上の速度で動かしながらパックを強く打つ（フリック・スマッシュする）** と、自動的にゲージを全消費して必殺技が発動します。（ゆっくり当てた場合は発動せず温存されます）。
* **必殺技の効果:**
  * **超加速:** 通常の速度上限（スピードリミッター）を無視し、通常の1.5〜2倍の圧倒的な超高速でパックが弾き飛ばされます。
  * **視覚・音響演出:** 
    * パックに炎のような「軌跡（パーティクルトレイル）」の描画が追加されます。
    * 発動時および壁への衝突時に、画面全体が揺れる「スクリーンシェイク効果」を実装します。
    * ド派手な専用の重低音・爆発音（SE）が鳴ります。

## 13. 追加開発フェーズ (UX改善 - ロビー・待機画面)
* **フェーズ 13: ルーム待機画面（ロビー）の実装**
  現状の「2人揃うと即座にゲームが開始される」仕様を変更し、参加者が揃ったことを確認してからゲームを開始できるように待機画面（ロビー）を導入します。

### 13.1. 画面フローとUI仕様
* **新しい画面フロー:**
  `タイトル画面` -> `ルーム待機画面（ロビー）` -> (開始操作) -> `ゲーム画面`
* **待機画面（Lobby UI）の要素:**
  * ルームIDの表示（他の人に共有しやすくする）。
  * 現在参加しているプレイヤーのリスト（プレイヤー名、担当ポジション、色）。
  * 参加人数状況（例: 2/4 人参加中）。
* **「ゲーム開始」ボタン:**
  * ルーム内に2人以上参加している場合のみ「ゲームを開始する」ボタンが有効になります。
  * アーケードの乱入対戦のように、ルーム内の**誰でも**開始ボタンを押すことができる仕様とします（シンプル化のため厳密なホスト権限は設けない）。

### 13.2. 通信と状態管理（サーバーサイド）
* **自動開始の廃止:**
  * `server.js` 側の「2人以上揃ったら自動で `status = 'PLAYING'` にする」処理を削除します。
* **ステートとイベント:**
  * サーバーはルームの状態として `LOBBY`（待機中）と `PLAYING`（試合中）を厳密に分けます。
  * クライアントは定期的なステート受信（または専用の `room_update` イベント）により、ロビーの参加者リストをリアルタイムに更新します。
  * クライアントから `start_game` イベントを送信し、サーバー側で人数チェック（2人以上）をクリアすればゲームを開始（状態を `PLAYING` へ移行）し、全クライアントにゲーム画面への遷移を指示します。

## 14. 追加開発フェーズ (UI/UX - ステージ選択機能)
* **フェーズ 14: ジャンル別ステージと選択UIの実装**
  待機画面（ロビー）で試合を行うステージ（背景デザイン・カラーテーマ）を選択できるようにし、ゲームの見た目を劇的に変化させます。

### 14.1. ステージの種類（ジャンル）
AI画像生成機能を活用し、3種類の異なるテーマの背景画像・カラーパレットを用意します。
1. **Cyberpunk (サイバーパンク) - デフォルト:**
   * 現在のネオン調のデザイン。暗い背景にシアンやマゼンタのライン。
2. **Retro Arcade (レトロアーケード):**
   * 80年代シンセウェイヴ調。ワイヤーフレームのグリッドや夕日をモチーフにしたネオンオレンジ系のデザイン。
3. **Fantasy Ice (ファンタジー氷):**
   * 凍った湖と魔法の陣をモチーフにしたデザイン。氷のような透明感のあるブルーやホワイトのカラーテーマ。

### 14.2. ロビー画面のUI拡張
* **ステージ選択UI:**
  * ロビー画面内に「ステージ選択」エリアを追加し、3つのステージのサムネイル（またはボタン）を配置します。
  * アーケードの「多数決」や「早い者勝ち」のように、**誰でも**ステージを変更できる仕様とします。
* **選択の同期:**
  * 誰かがステージを変更すると、サーバーを通じてリアルタイムに他のプレイヤーのロビー画面にも「現在選択されているステージ」が同期・反映されます。

### 14.3. ゲーム画面の描画変更
* **背景画像の適用:**
  * Canvasの描画（`drawBoard`）時に、単なる黒背景ではなく、選択されたステージに応じた背景画像を一番下に描画します（画像は透明度を調整し、見やすくする）。
* **テーマカラーの切り替え:**
  * ステージに応じて、壁の線の色やセンターラインの色を切り替えます（例: Cyberpunkならシアン、Retroならオレンジ、Iceならホワイト）。
